{% extends 'base.html.twig' %}

{% block title %}Détail Event{% endblock %}

{% block body %}

<h2>{{ event.name }}</h2>

<section class="gallery">
    {% for image in images %}
    <div class="gallery__item">
        <input type="radio" id="{{image.id}}" checked name="gallery" class="gallery__selector"/>
          <img class="gallery__img" src="{{ asset('uploads/images/' ~ image.media)}}" alt=""/>
        <label for="{{image.id}}" class="gallery__thumb">
            <img src="{{ asset('uploads/images/' ~ image.media)}}" alt=""/>
        </label>
    </div>
    {% endfor %}
</section>

{% endblock %}